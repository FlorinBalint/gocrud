// Code generated by entitygen. DO NOT EDIT.

syntax = "proto3";

package library.v1;

import "library/v1/book.proto";
import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/api/field_behavior.proto";
import "google/api/resource.proto";
import "proto/v1/query.proto";
import "google/protobuf/field_mask.proto";
import "google/protobuf/empty.proto";

option go_package = "github.com/example/library/v1;libraryv1";

// ListBookRequest is the request message for BookService.ListBook.
message ListBookRequest {
  // The parent resource to list under.
  string parent = 1 [
    (google.api.field_behavior) = REQUIRED,
    (google.api.resource_reference).child_type = "library.v1/Book"
  ];

  // Root of the WHERE-clause expression tree.
  // Absent means no filtering (all rows match).
  gocrud.v1.Filter filter = 2;

  // Sort order. Entries are applied left-to-right (primary sort first).
  repeated gocrud.v1.OrderBy order_by = 3;

  // Pagination parameters. Absent means the server returns a default first page.
  gocrud.v1.Pagination pagination = 4;

  // If true, the response will include the total number of rows matching the
  // filter, regardless of pagination.
  bool include_total = 5;
}

// ListBookResponse is the response message for BookService.ListBook.
message ListBookResponse {
  // The list of Book resources.
  repeated Book books = 1;

  // A token, which can be sent as 'page_token' to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;

  // A token, which can be sent as 'page_token' to retrieve the previous page.
  // If this field is omitted, there are no previous pages.
  string prev_page_token = 3;

  // The total number of items, if requested.
  int64 total_size = 4;
}

// GetBookRequest is the request message for BookService.GetBook.
message GetBookRequest {
  // The name of the resource to retrieve.
  string name = 1 [
    (google.api.field_behavior) = REQUIRED,
    (google.api.resource_reference).type = "library.v1/Book"
  ];
}

// CreateBookRequest is the request message for BookService.CreateBook.
message CreateBookRequest {
  // The parent resource where the Book will be created.
  string parent = 1 [
    (google.api.field_behavior) = REQUIRED,
    (google.api.resource_reference).child_type = "library.v1/Book"
  ];

  // The book_id of the Book to create.
  string book_id = 2;

  // The Book resource to create.
  Book book = 3 [(google.api.field_behavior) = REQUIRED];
}

// UpdateBookRequest is the request message for BookService.UpdateBook.
message UpdateBookRequest {
  // The Book resource to update.
  Book book = 1 [(google.api.field_behavior) = REQUIRED];

  // The list of fields to update.
  google.protobuf.FieldMask update_mask = 2 [(google.api.field_behavior) = OPTIONAL];

}

// UpsertBookRequest is the request message for BookService.UpsertBook.
message UpsertBookRequest {
  // The Book resource to upsert.
  Book book = 1 [(google.api.field_behavior) = REQUIRED];

  // The list of fields to update.
  google.protobuf.FieldMask update_mask = 2 [(google.api.field_behavior) = OPTIONAL];

}

// DeleteBookRequest is the request message for BookService.DeleteBook.
message DeleteBookRequest {
  // The name of the resource to delete.
  string name = 1 [
    (google.api.field_behavior) = REQUIRED,
    (google.api.resource_reference).type = "library.v1/Book"
  ];

  // If true, performs a cascading delete of the resource and all its children.
  bool force_cascade = 2 [(google.api.field_behavior) = OPTIONAL];

  // If true, the request will succeed even if the resource does not exist.
  bool allow_missing = 3;
}

// BookService provides CRUD operations for Book entities.
service BookService {
  option (google.api.default_host) = "library.v1.example.com";
  // GetBook retrieves a Book by its resource name.
  rpc GetBook(GetBookRequest) returns (Book) {
    option (google.api.http) = {
      get: "/library/v1/books/{name=**}"
    };
    option (google.api.method_signature) = "name";
  }

  // ListBook lists Book entities.
  rpc ListBook(ListBookRequest) returns (ListBookResponse) {
    option (google.api.http) = {
      get: "/library/v1/books"
    };
    option (google.api.method_signature) = "parent";
  }

  // CreateBook creates a new Book.
  rpc CreateBook(CreateBookRequest) returns (Book) {
    option (google.api.http) = {
      post: "/library/v1/books"
      body: "book"
    };
    option (google.api.method_signature) = "parent,book";
  }

  // UpdateBook updates a Book.
  rpc UpdateBook(UpdateBookRequest) returns (Book) {
    option (google.api.http) = {
      patch: "/library/v1/books"
      body: "book"
    };
    option (google.api.method_signature) = "book,update_mask";
  }

  // UpsertBook upserts a Book.
  rpc UpsertBook(UpsertBookRequest) returns (Book) {
    option (google.api.http) = {
      put: "/library/v1/books"
      body: "book"
    };
    option (google.api.method_signature) = "book,update_mask";
  }

  // DeleteBook deletes a Book.
  rpc DeleteBook(DeleteBookRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {
      delete: "/library/v1/books/{name=**}"
    };
    option (google.api.method_signature) = "name";
  }
}
