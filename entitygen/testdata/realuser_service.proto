// Code generated by entitygen. DO NOT EDIT.

syntax = "proto3";

package testdata.v1;

import "entitygen/testdata/entities.proto";
import "google/api/annotations.proto";
import "google/api/client.proto";
import "google/api/field_behavior.proto";
import "google/api/resource.proto";
import "proto/v1/query.proto";
import "google/protobuf/field_mask.proto";
import "google/protobuf/empty.proto";

option go_package = "github.com/FlorinBalint/gocrud/entitygen/testdata;testdata";

// ListRealUsersRequest is the request message for RealUserService.ListRealUsers.
message ListRealUsersRequest {
  // The parent resource to list under.
  string parent = 1 [
    (google.api.field_behavior) = REQUIRED,
    (google.api.resource_reference).child_type = "testdata.v1/RealUser"
  ];

  // Root of the WHERE-clause expression tree.
  // Absent means no filtering (all rows match).
  gocrud.v1.Filter filter = 2;

  // Sort order. Entries are applied left-to-right (primary sort first).
  repeated gocrud.v1.OrderBy order_by = 3;

  // Pagination parameters. Absent means the server returns a default first page.
  gocrud.v1.Pagination pagination = 4;

  // If true, the response will include the total number of rows matching the
  // filter, regardless of pagination.
  bool include_total = 5;
}

// ListRealUsersResponse is the response message for RealUserService.ListRealUsers.
message ListRealUsersResponse {
  // The list of RealUser resources.
  repeated RealUser real_users = 1;

  // A token, which can be sent as 'page_token' to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;

  // A token, which can be sent as 'page_token' to retrieve the previous page.
  // If this field is omitted, there are no previous pages.
  string prev_page_token = 3;

  // The total number of items, if requested.
  int64 total_size = 4;
}

// GetRealUserRequest is the request message for RealUserService.GetRealUser.
message GetRealUserRequest {
  // The name of the resource to retrieve.
  string name = 1 [
    (google.api.field_behavior) = REQUIRED,
    (google.api.resource_reference).type = "testdata.v1/RealUser"
  ];
}

// CreateRealUserRequest is the request message for RealUserService.CreateRealUser.
message CreateRealUserRequest {
  // The parent resource where the RealUser will be created.
  string parent = 1 [
    (google.api.field_behavior) = REQUIRED,
    (google.api.resource_reference).child_type = "testdata.v1/RealUser"
  ];

  // The id of the RealUser to create.
  string id = 2;

  // The RealUser resource to create.
  RealUser real_user = 3 [(google.api.field_behavior) = REQUIRED];
}

// UpdateRealUserRequest is the request message for RealUserService.UpdateRealUser.
message UpdateRealUserRequest {
  // The RealUser resource to update.
  RealUser real_user = 1 [(google.api.field_behavior) = REQUIRED];

  // The list of fields to update.
  google.protobuf.FieldMask update_mask = 2 [(google.api.field_behavior) = OPTIONAL];

}

// UpsertRealUserRequest is the request message for RealUserService.UpsertRealUser.
message UpsertRealUserRequest {
  // The RealUser resource to upsert.
  RealUser real_user = 1 [(google.api.field_behavior) = REQUIRED];

  // The list of fields to update.
  google.protobuf.FieldMask update_mask = 2 [(google.api.field_behavior) = OPTIONAL];

}

// DeleteRealUserRequest is the request message for RealUserService.DeleteRealUser.
message DeleteRealUserRequest {
  // The name of the resource to delete.
  string name = 1 [
    (google.api.field_behavior) = REQUIRED,
    (google.api.resource_reference).type = "testdata.v1/RealUser"
  ];

  // If true, performs a cascading delete of the resource and all its children.
  bool force_cascade = 2 [(google.api.field_behavior) = OPTIONAL];

  // If true, the request will succeed even if the resource does not exist.
  bool allow_missing = 3;
}

// RealUserService provides CRUD operations for RealUser entities.
service RealUserService {
  option (google.api.default_host) = "testdata.v1.example.com";
  // GetRealUser retrieves a RealUser by its resource name.
  rpc GetRealUser(GetRealUserRequest) returns (RealUser) {
    option (google.api.http) = {
      get: "/testdata/v1/real_users/{name=**}"
    };
    option (google.api.method_signature) = "name";
  }

  // ListRealUsers lists RealUser entities.
  rpc ListRealUsers(ListRealUsersRequest) returns (ListRealUsersResponse) {
    option (google.api.http) = {
      get: "/testdata/v1/real_users"
    };
    option (google.api.method_signature) = "parent";
  }

  // CreateRealUser creates a new RealUser.
  rpc CreateRealUser(CreateRealUserRequest) returns (RealUser) {
    option (google.api.http) = {
      post: "/testdata/v1/real_users"
      body: "real_user"
    };
    option (google.api.method_signature) = "parent,real_user";
  }

  // UpdateRealUser updates a RealUser.
  rpc UpdateRealUser(UpdateRealUserRequest) returns (RealUser) {
    option (google.api.http) = {
      patch: "/testdata/v1/real_users"
      body: "real_user"
    };
    option (google.api.method_signature) = "real_user,update_mask";
  }

  // UpsertRealUser upserts a RealUser.
  rpc UpsertRealUser(UpsertRealUserRequest) returns (RealUser) {
    option (google.api.http) = {
      put: "/testdata/v1/real_users"
      body: "real_user"
    };
    option (google.api.method_signature) = "real_user,update_mask";
  }

  // DeleteRealUser deletes a RealUser.
  rpc DeleteRealUser(DeleteRealUserRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {
      delete: "/testdata/v1/real_users/{name=**}"
    };
    option (google.api.method_signature) = "name";
  }
}
